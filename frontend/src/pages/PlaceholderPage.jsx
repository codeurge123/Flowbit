export function PlaceholderPage({ title }) {
  return (
    <section className="content-section empty-project">
      <h1>{title}</h1>
      <p>This workspace is connected and ready for project data.</p>
    </section>
  );
}
